# PMS Agent Rules

- Always update `.agents/IMPLEMENTATION_TODO.md` and `SESSION_HANDOFF.md` at session end and when requirements change
- Never use the Vecktrix CMS Neon database or CMS Prisma models
- UI must follow `.agents/DESIGN_SYSTEM.md` (Vecktrix website tokens)
- Every mutation must assert RBAC
- Clients must not get task review/comment capabilities
- Read Auth.js skill under `.cursor/skills/gocallum-nextjs16-agent-skills-authjs-skills/` before auth work
