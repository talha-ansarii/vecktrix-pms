# Database (dedicated Neon Postgres — NOT the CMS database)
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Auth.js
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# Google OAuth (optional)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Lead intake API (website → PMS)
LEAD_INTAKE_SECRET="shared-secret-for-intake-endpoints"

# Vercel Blob (project file uploads)
BLOB_READ_WRITE_TOKEN=""
